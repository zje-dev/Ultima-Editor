#include <string>
#include <vector>

std::string get_cas (int);
void convert_xpm (int, int, std::string, int, int, int, int, int, bool, bool);
void castle_xpm (int, std::string);
void map_xpm (const char*);
